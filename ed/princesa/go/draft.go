package main
import "fmt"
func main() {
    var tamanho, posicao int
    fmt.Scan(&tamanho, &posicao)
    
    vet_elementos := make([]int, tamanho)
    vet_vivos := make([]int, posicao)
    
    if {

    }

}
