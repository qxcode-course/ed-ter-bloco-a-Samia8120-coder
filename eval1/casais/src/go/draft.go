package main
import "fmt"

func coisa(tcasais int){
    var animal int
    contpar := 0
    descasados:= make([]int, tcasais)
    for i := 0; i < tcasais; i++{
        fmt.Scan(&animal)
        descasados[i] = animal
        if  {
                
        }
    }
}

func main() {
    var tcasais int
    fmt.Scan(&tcasais)
}
